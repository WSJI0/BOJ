//14464번 소가 길을 건너간 이유 4 (미완성)

#include <bits/stdc++.h>
using namespace std;

int main(void){
    ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    int c, n, a, b;
    cin>>c>>n;
    vector<int> t(c);
    for(int i=0; i<c; i++) cin>>t[i];
    sort(t.begin(), t.end());
    for(int i=0; i<n; i++){
        cin>>a>>b;
        
    }
}