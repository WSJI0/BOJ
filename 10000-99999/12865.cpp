//12865번 평범한 배낭

#include <bits/stdc++.h>
using namespace std;

int main(void){
    ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    int n, k, dp[100000][100]={0};
    cin>>n>>k;
    vector<pair<int, int>> w(n);
    for(int i=0; i<n; i++)
        cin>>w[i].first>>w[i].second;
    
    
}