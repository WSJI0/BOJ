// 11003번 최솟값 찾기

#include <bits/stdc++.h>
using namespace std;

int main(void){
    ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    int n, l, a;
    cin>>n>>l;

    deque<int> d;
    for(int i=0; i<n; i++){
        cin>>a;
        d.push_back(a);
        if(d.size()>l){
            
        }
    }
}